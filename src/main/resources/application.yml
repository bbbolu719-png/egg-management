server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/egg_management?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: egg123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always
      continue-on-error: true
  jackson:
    property-naming-strategy: SNAKE_CASE
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.egg.management.entity
  global-config:
    db-config:
      id-type: auto
logging:
  level:
    com.egg.management: debug
